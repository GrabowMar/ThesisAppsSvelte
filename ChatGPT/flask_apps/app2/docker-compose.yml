version: '3.8'
services:
    backend:
        image: app2-backend
        ports:
            - "5002:5000"
        restart: always

    frontend:
        image: app2-frontend
        ports:
            - "5175:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
