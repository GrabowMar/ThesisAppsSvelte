version: '3.8'
services:
    backend:
        image: app13-backend
        ports:
            - "5013:5000"
        restart: always

    frontend:
        image: app13-frontend
        ports:
            - "5186:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
