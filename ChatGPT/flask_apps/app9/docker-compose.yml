version: '3.8'
services:
    backend:
        image: app9-backend
        ports:
            - "5009:5000"
        restart: always

    frontend:
        image: app9-frontend
        ports:
            - "5182:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
