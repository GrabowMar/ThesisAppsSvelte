version: '3.8'
services:
    backend:
        image: app10-backend
        ports:
            - "5010:5000"
        restart: always

    frontend:
        image: app10-frontend
        ports:
            - "5183:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
