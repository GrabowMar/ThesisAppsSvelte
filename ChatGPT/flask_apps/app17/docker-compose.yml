version: '3.8'
services:
    backend:
        image: app17-backend
        ports:
            - "5017:5000"
        restart: always

    frontend:
        image: app17-frontend
        ports:
            - "5190:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
