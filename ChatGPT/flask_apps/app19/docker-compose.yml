version: '3.8'
services:
    backend:
        image: app19-backend
        ports:
            - "5019:5000"
        restart: always

    frontend:
        image: app19-frontend
        ports:
            - "5192:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
