version: '3.8'
services:
    backend:
        image: app1-backend
        ports:
            - "5002"
        restart: always

    frontend:
        image: app1-frontend
        ports:
            - "5174"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
