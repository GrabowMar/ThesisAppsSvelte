version: '3.8'
services:
    backend:
        image: app5-backend
        ports:
            - "5005:5000"
        restart: always

    frontend:
        image: app5-frontend
        ports:
            - "5178:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
