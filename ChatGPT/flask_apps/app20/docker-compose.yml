version: '3.8'
services:
    backend:
        image: app20-backend
        ports:
            - "5020:5000"
        restart: always

    frontend:
        image: app20-frontend
        ports:
            - "5193:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
