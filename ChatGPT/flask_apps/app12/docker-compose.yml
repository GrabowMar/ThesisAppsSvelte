version: '3.8'
services:
    backend:
        image: app12-backend
        ports:
            - "5012:5000"
        restart: always

    frontend:
        image: app12-frontend
        ports:
            - "5185:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
