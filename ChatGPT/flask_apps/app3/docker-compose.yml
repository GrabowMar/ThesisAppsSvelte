version: '3.8'
services:
    backend:
        image: app3-backend
        ports:
            - "5003:5000"
        restart: always

    frontend:
        image: app3-frontend
        ports:
            - "5176:5173"
        volumes:
            - ./frontend:/app
        depends_on:
            - backend
        restart: always
