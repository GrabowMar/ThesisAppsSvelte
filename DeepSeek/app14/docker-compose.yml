version: '3.8'
services:
    backend:
        build: ./backend
        container_name: deepseek_backend_5507
        ports:
            - "5507:5507"
        volumes:
            - ./backend:/app
        restart: always
        environment:
            - PYTHONUNBUFFERED=1 

    frontend:
        build: ./frontend
        container_name: deepseek_frontend_6007
        ports:
            - "6007:6007"
        volumes:
            - ./frontend:/app
            - /app/node_modules
        environment:
            - PORT=6007
        restart: always
        depends_on:
            - backend

